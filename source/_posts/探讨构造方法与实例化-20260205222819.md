---
title: Java static 关键字 - 学习总结
date: 2026-02-05
tags: [java, 基础]
---

# Java static 关键字 - 学习总结

**学习日期**：2026-02-05  
**学习时长**：约 30 分钟  
**学习方法**：费曼学习法（问答互动）

---

## 一、今日学习内容汇总

### 1. static 关键字的修饰范围

**知识点**：
- **静态变量**（static variables）：也叫类变量，属于类本身，而不是某个对象实例
- **静态方法**（static methods）：属于类，可以直接通过类名调用
- **静态代码块**（static blocks）：用于类加载时执行一次的初始化操作
- **静态内部类**（static nested classes）：嵌套在另一个类中的静态类
- **静态导入**（static import）：如 `import static java.lang.Math.*;`

**实际应用场景**：
- ✅ 静态变量：用于共享状态（如统计对象创建次数）
- ✅ 静态方法：工具方法（如 `Math.random()`）
- ✅ 静态代码块：初始化静态资源、加载驱动
- ✅ 静态内部类：逻辑上属于外部类但不依赖实例

### 2. static 方法不能访问非静态成员的原因

**知识点**：
- 非 static 的成员变量和方法属于某个具体的对象实例，只有在创建对象（new）之后才存在
- static 方法或变量属于类本身，在类被加载到 JVM 时就存在了，不需要创建对象
- 当 static 方法被调用时，可能还没有任何该类的对象被创建，因此无法访问非静态成员

**实际应用场景**：
- ✅ 避免在静态方法中直接访问实例变量
- ✅ 如需访问实例成员，需要先创建对象实例

### 3. static 变量的应用：统计对象创建次数

**知识点**：
- 使用 static 变量作为计数器，因为它是类级别的，所有对象共享
- 在构造方法中对 static 变量进行自增
- 提供静态方法获取当前计数

**示例代码**：
```java
public class Demo {
    static int num = 0;
    
    Demo() {
        num++;
    }
    
    static void getCount() {
        System.out.println("当前对象数量是：" + num);
    }
    
    public static void main(String[] args) {
        Demo demo = new Demo();
        Demo demo1 = new Demo();
        getCount(); // 输出：当前对象数量是：2
    }
}
```

**实际应用场景**：
- ✅ 统计系统中某个类的实例数量
- ✅ 实现单例模式的计数器

### 4. static 代码块的执行时机

**知识点**：
- 静态代码块在**类被加载到 JVM 时**执行，而不是在创建对象时
- 只执行一次，即使没有创建任何对象
- 早于 main 方法和构造方法执行

**示例代码**：
```java
public class Demo {
    static {
        System.out.println("静态代码块执行了！");
    }
    
    public Demo() {
        System.out.println("构造方法执行了！");
    }
    
    public static void main(String[] args) {
        System.out.println("开始创建对象...");
        new Demo();
        new Demo();
    }
}
// 输出：
// 静态代码块执行了！
// 开始创建对象...
// 构造方法执行了！
// 构造方法执行了！
```

**实际应用场景**：
- ✅ 初始化静态资源（如加载配置文件）
- ✅ 加载数据库驱动
- ✅ 设置全局配置

### 5. main 方法必须是 static 的原因

**知识点**：
- JVM 在启动程序时，并没有该类的任何实例对象
- 当运行 `java MyClass` 时，JVM 会加载 MyClass 类并直接调用 `public static void main(String[] args)` 方法
- 如果 main 方法不是 static 的，JVM 无法知道该用哪个对象来调用它

**实际应用场景**：
- ✅ 所有 Java 应用程序的入口点

### 6. 代码块的执行顺序

**知识点**：
- **静态代码块**：类加载时执行，只执行一次
- **构造代码块**（{}）：每次创建对象时执行，在构造方法之前
- **构造方法**：每次创建对象时执行，在构造块之后

**执行顺序**：静态代码块 > 构造块 > 构造方法

**示例代码**：
```java
class A {
    static {
        System.out.println("静态块");
    }
    
    {
        System.out.println("构造块");
    }
    
    public A() {
        System.out.println("构造方法");
    }
    
    public static void main(String[] args) {
        new A();
        new A();
    }
}
// 输出：
// 静态块
// 构造块
// 构造方法
// 构造块
// 构造方法
```

**实际应用场景**：
- ✅ 理解 Java 类的初始化过程
- ✅ 合理安排代码块的执行顺序

## 二、今日记忆口诀汇总

1. **static 修饰范围**：
   > **static 修饰五大件，变量方法代码块**
   > **内部类和静态导，类级共享是关键**

2. **static 方法访问限制**：
   > **静态方法无实例，非静成员不能用**
   > **类加载时就存在，对象实例还未生**

3. **static 变量应用**：
   > **静态变量共享用，对象计数最典型**
   > **构造方法自增一，类名调用取统计**

4. **static 代码块**：
   > **静态代码块加载时，只执行一次记心间**
   > **早于 main 和构造，初始化资源最适合**

5. **main 方法必须 static**：
   > **main 方法是入口，JVM 调用无对象**
   > **static 关键字不可少，否则启动就报错**

6. **代码块执行顺序**：
   > **静态块先执行，构造块在构造前**
   > **静态只执行一次，每次 new 都执行构造块**

## 三、学习时长统计

- **开始时间**：2026-02-05 22:28
- **结束时间**：2026-02-05 22:58
- **总时长**：约 30 分钟

## 四、明日学习计划

### 建议复习内容
1. 复习今日学习的 static 关键字知识点，重点记忆口诀
2. 练习编写代码，使用 static 变量统计对象创建次数
3. 重点复习代码块的执行顺序

### 建议学习内容
1. **Java 集合框架**：List、Set、Map 的区别和使用
2. **泛型**：泛型的使用、通配符、类型擦除
3. **Java 8 新特性**：Lambda 表达式、Stream API

### 需要补充的知识点
1. **静态内部类与非静态内部类的区别**
2. **静态导入的使用场景和注意事项**
3. **static 与 final 结合使用**
4. **静态方法与实例方法的性能对比**

## 五、学习心得

通过今天的学习，我对 Java 中 static 关键字有了更深入的理解。以前只知道 static 可以修饰变量和方法，现在清楚了它还可以修饰代码块、内部类和静态导入。特别是 static 方法不能访问非静态成员的原因、static 代码块的执行时机以及代码块的执行顺序，这些都是实际开发中容易混淆的地方。

费曼学习法的问答方式让我能够主动思考，而不是被动接受知识。虽然有些问题一开始回答不够完整，甚至有错误，但通过老师的详细讲解和纠正，我能够更好地理解和记忆这些知识点。记忆口诀的总结也让我能够快速回顾和巩固所学内容。

明天计划学习 Java 集合框架，继续深入理解 Java 的核心特性。