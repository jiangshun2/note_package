---
title: HashSet、LinkedHashSet、TreeSet - 学习总结
date: 2026-02-25
tags: [java, 基础]
---

# HashSet、LinkedHashSet、TreeSet - 学习总结

**学习日期**：2026-02-25  
**学习时长**：约 15 分钟  
**学习方法**：费曼学习法（问答互动）

---

## 一、今日学习内容汇总

### 1. HashSet

**知识点**：
- **底层实现**：哈希表（数组 + 链表/红黑树）
- **特点**：
  - ❌ 无序（不保证元素的存储顺序）
  - ❌ 不可重复（保证元素唯一性）
  - ❌ 无索引（Set 接口都没有索引）
  - ✅ 允许一个 null 值
- **性能**：添加、删除、查询都是 O(1)，性能最快

**去重机制**（重要！）：
```
添加元素时的判断流程：
1. 计算元素的 hashCode() → 得到哈希值
2. 根据哈希值确定存储位置
3. 如果该位置没有元素 → 直接添加
4. 如果该位置有元素 → 遍历链表/红黑树，用 equals() 比较
   - equals() 返回 false → 添加
   - equals() 返回 true → 不添加（重复）
```

**关键方法**：
- 必须同时重写 `hashCode()` 和 `equals()` 方法
- 只重写 `equals()`：两个对象相等但哈希值不同，会被存储在不同位置，导致重复
- 只重写 `hashCode()`：两个对象哈希值相同但不相等，会被误判为重复

**记忆口诀**：
> **HashSet 哈希表，无序不可重复**
> **去重靠哈希，equals 定乾坤**

**实际应用场景**：
- ✅ 只需要去重，不关心顺序
- ✅ 对性能要求极高
- ✅ 用户标签系统、权限集合、数据去重

**我的理解**：
HashSet 是最常用的 Set 实现，底层使用哈希表，所以添加、删除、查询都非常快，时间复杂度是 O(1)。但是元素是无序的，不能保证存储顺序。HashSet 通过 hashCode() 和 equals() 方法来判断元素是否重复，所以如果要让自定义类的对象能够正确去重，必须同时重写这两个方法。HashSet 适合只需要去重而不关心顺序的场景，比如用户标签系统，一个标签不能重复添加。

---

### 2. LinkedHashSet

**知识点**：
- **底层实现**：哈希表 + 双向链表
- **特点**：
  - ✅ 有序（按插入顺序）
  - ❌ 不可重复（保证元素唯一性）
  - ❌ 无索引（Set 接口都没有索引）
  - ✅ 允许一个 null 值
- **性能**：添加、删除、查询都是 O(1)，略慢于 HashSet（因为需要维护链表）

**与 HashSet 的区别**：
- LinkedHashSet 比 HashSet 多了一个双向链表，用于维护插入顺序
- 在添加元素时，除了存储到哈希表，还会在链表末尾添加节点
- 在遍历时，按照链表的顺序（即插入顺序）遍历

**记忆口诀**：
> **LinkedHashSet 插序，哈希链表双保险**
> **有序不重复，历史记录用**

**实际应用场景**：
- ✅ 需要去重，且关心插入顺序
- ✅ 历史记录、最近访问列表
- ✅ LRU 缓存的实现
- ✅ 购物车商品列表（去重且保持添加顺序）

**我的理解**：
LinkedHashSet 在 HashSet 的基础上增加了一个双向链表来维护插入顺序，所以元素是按照插入顺序排列的。这使得 LinkedHashSet 既能去重，又能保持插入顺序，非常适合用于需要记录历史记录的场景，比如浏览器的浏览历史、最近访问的商品列表等。虽然性能略低于 HashSet，但在需要保持顺序的场景下是非常好的选择。

---

### 3. TreeSet

**知识点**：
- **底层实现**：红黑树（注意：不是哈希表！）
- **特点**：
  - ✅ 有序（按自然顺序或自定义排序）
  - ❌ 不可重复（保证元素唯一性）
  - ❌ 无索引（Set 接口都没有索引）
  - ❌ 不允许 null 值
- **性能**：添加、删除、查询都是 O(log n)，性能较慢

**去重和排序机制**（重要！）：
```
添加元素时的判断流程：
1. 使用 compareTo() 或 compare() 方法比较元素
2. 返回 0 → 元素相同，不添加（保证唯一性）
3. 返回负数 → 新元素较小，插入到左边
4. 返回正数 → 新元素较大，插入到右边
5. 通过红黑树的平衡特性，保证有序性和唯一性
```

**关键方法**：
- TreeSet **不使用** hashCode() 和 equals() 方法
- 使用 compareTo() 方法（自然排序）或 compare() 方法（比较器排序）

**排序方式**：

**方式 1：自然排序（Comparable）**
- 让元素类实现 `Comparable` 接口
- 重写 `compareTo()` 方法
- 适用于元素类有固定的排序规则

**方式 2：比较器排序（Comparator）**
- 创建 `Comparator` 对象（匿名内部类或 Lambda）
- 在创建 TreeSet 时传入比较器
- 适用于需要临时改变排序规则，或无法修改元素类
- **优先级高于自然排序**

**记忆口诀**：
> **TreeSet 红黑树，有序不重复**
> **排序两方式，Comparable 比较器**
> **性能 O(log n)，排行榜上用**

**实际应用场景**：
- ✅ 需要排序的集合
- ✅ 排行榜、成绩排名、商品价格排序
- ✅ 需要快速查找范围（如查找某个区间内的元素）
- ❌ 只关心去重，不关心顺序（应该用 HashSet）
- ❌ 需要插入顺序（应该用 LinkedHashSet）

**我的理解**：
TreeSet 底层使用红黑树，元素会按照自然顺序或自定义排序规则排列，所以是有序的。TreeSet 通过 compareTo() 或 compare() 方法来判断元素是否重复和确定排序位置，而不是使用 hashCode() 和 equals() 方法。TreeSet 适合需要排序的场景，比如排行榜、成绩排名等。但是 TreeSet 的性能不如 HashSet 和 LinkedHashSet，因为红黑树的插入、删除、查询时间复杂度是 O(log n)，而哈希表是 O(1)。

---

## 二、三种 Set 的对比

| 特性 | HashSet | LinkedHashSet | TreeSet |
|------|---------|---------------|---------|
| **底层结构** | 哈希表（数组+链表/红黑树） | 哈希表 + 双向链表 | 红黑树 |
| **是否有序** | ❌ 无序 | ✅ 有序（按插入顺序） | ✅ 有序（按排序规则） |
| **是否允许 null** | ✅ 允许一个 null | ✅ 允许一个 null | ❌ 不允许 null |
| **是否允许重复** | ❌ 不允许 | ❌ 不允许 | ❌ 不允许 |
| **是否有索引** | ❌ 无索引 | ❌ 无索引 | ❌ 无索引 |
| **添加/删除/查询** | ⚡ O(1) 最快 | ⚡ O(1) 较快 | 🐢 O(log n) 较慢 |
| **遍历** | ⚡ O(n) | ⚡ O(n) | ⚡ O(n) |

**选择决策树**：
```
需要排序？
├─ 是 → TreeSet（按自然顺序或自定义排序）
└─ 否 → 需要保持插入顺序？
    ├─ 是 → LinkedHashSet（按插入顺序）
    └─ 否 → HashSet（无序，性能最好）
```

---

## 三、今日记忆口诀汇总

1. **HashSet**：
   > **HashSet 哈希表，无序不可重复**
   > **去重靠哈希，equals 定乾坤**

2. **LinkedHashSet**：
   > **LinkedHashSet 插序，哈希链表双保险**
   > **有序不重复，历史记录用**

3. **TreeSet**：
   > **TreeSet 红黑树，有序不重复**
   > **排序两方式，Comparable 比较器**
   > **性能 O(log n)，排行榜上用**

4. **选择原则**：
   > **需要排序用 TreeSet，需要插序用 LinkedHashSet**
   > **只去重用 HashSet，性能优先选它**

---

## 四、学习时长统计

- **开始时间**：2026-02-25
- **结束时间**：2026-02-25
- **总时长**：约 15 分钟

---

## 五、明日学习计划

### 建议复习内容
1. 复习今日学习的 HashSet、LinkedHashSet、TreeSet，重点记忆口诀
2. 练习编写代码，使用不同的 Set 实现类解决实际问题
3. 重点复习 HashSet 的去重机制（hashCode 和 equals）
4. 重点复习 TreeSet 的两种排序方式（Comparable 和 Comparator）

### 建议学习内容
1. **Map 接口**：HashMap、LinkedHashMap、TreeMap 的区别和使用
2. **Map 的遍历方式**：keySet()、entrySet()、values()
3. **Collections 工具类**：常用的静态方法（sort、reverse、shuffle 等）
4. **Java 8 新特性**：Stream API、Optional 类

### 需要补充的知识点
1. **HashSet 的扩容机制**：初始容量、负载因子、扩容时机
2. **红黑树的基本原理**：红黑树的平衡特性、旋转操作
3. **Comparable 和 Comparator 的区别**：何时使用哪种方式
4. **Set 接口的常用方法**：add、remove、contains、size、isEmpty 等
5. **Set 的遍历方式**：for-each 循环、Iterator 迭代器

---

## 六、学习心得

通过今天的学习，我对 HashSet、LinkedHashSet 和 TreeSet 三个 Set 接口的实现类有了更深入的理解。以前只知道它们都是 Set 的实现，都能去重，现在清楚了它们的底层实现原理、特点和适用场景。

HashSet 是最常用的 Set 实现，底层使用哈希表，性能最好，但是无序。LinkedHashSet 在 HashSet 的基础上增加了一个双向链表来维护插入顺序，所以是有序的，但性能略低于 HashSet。TreeSet 底层使用红黑树，元素会按照自然顺序或自定义排序规则排列，所以是有序的，但性能不如前两者。

特别重要的是，HashSet 和 LinkedHashSet 使用 hashCode() 和 equals() 方法来判断元素是否重复，而 TreeSet 使用 compareTo() 或 compare() 方法。这个区别在面试中经常被问到，也是实际开发中需要注意的地方。

费曼学习法的问答方式让我能够主动思考，而不是被动接受知识。虽然有些问题一开始回答不够完整，甚至有错误，但通过老师的详细讲解和纠正，我能够更好地理解和记忆这些知识点。记忆口诀的总结也让我能够快速回顾和巩固所学内容。

明天计划学习 Map 接口，继续深入理解 Java 集合框架。Map 和 Set 是 Java 集合框架中最重要的两个接口，掌握好它们对于提高代码质量和开发效率非常重要。